# writeStream Operator

```scala
writeStream: DataStreamWriter[T]
```

`writeStream` creates a [DataStreamWriter](../DataStreamWriter.md) for persisting the result of a streaming query to an external data system
